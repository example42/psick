# File Managed by Puppet

define host{
    hostgroups <%= hostgroups %>
    host_name  <%= name %>
    alias      <%= short_alias %>
    address    <%= ip %>
    use        <%= use %>
    <% if ! (/^(none|)$/i =~ nagios_parent) then %> parents <%= nagios_parent %> <% end %>
}
