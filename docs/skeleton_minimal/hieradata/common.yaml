---
# Profiles key may be used to include profile classes
profiles:
  - '::profile::base'

# Sample data structures
sample_string: "Found in hieradata/common.yaml"
sample_array:
  - One
  - Two
sample_hash:
  one: 'string'
  two:
    nested: 'hash'

