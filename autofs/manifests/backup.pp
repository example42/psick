# Class: autofs::backup
#
# Backups autofs data directory and, optionally, logs (must be enabled)
# It's automatically included if $backup=yes
#
# Usage:
# include autofs::backup
#
class autofs::backup {

}
