---
psick::profiles::linux_classes:
  vagrant: psick::vagrant
  git: psick::git
  puppet_gems: psick::puppet::gems
  docker: psick::docker
  puppet_pdk: psick::puppet::pdk

# Override default plugins
psick::vagrant::default_plugins:
  - vagrant-cachier
  - vagrant-vbguest

# Install extra plugins
psick::vagrant::plugins:
  - vagrant-hostmanager

