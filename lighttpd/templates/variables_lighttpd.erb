# File Managed By Puppet

These are the variables used by the module lighttpd

User variables (unfiltered):
# $lighttpd_server = <%= scope.lookupvar('lighttpd_server') %>

User variables (after params.pp filter):
# $server = <%= scope.lookupvar('lighttpd::params::server') %>

General internal variables:
$packagename = <%= scope.lookupvar('lighttpd::params::packagename') %>
$servicename = <%= scope.lookupvar('lighttpd::params::servicename') %>
$processname = <%= scope.lookupvar('lighttpd::params::processname') %>
$hasstatus = <%= scope.lookupvar('lighttpd::params::hasstatus') %>
$configfile = <%= scope.lookupvar('lighttpd::params::configfile') %>
$configfile_mode = <%= scope.lookupvar('lighttpd::params::configfile_mode') %>
$configfile_owner = <%= scope.lookupvar('lighttpd::params::configfile_owner') %>
$configfile_group = <%= scope.lookupvar('lighttpd::params::configfile_group') %>
$configdir = <%= scope.lookupvar('lighttpd::params::configdir') %>
$initconfigfile = <%= scope.lookupvar('lighttpd::params::initconfigfile') %>
$pidfile = <%= scope.lookupvar('lighttpd::params::pidfile') %>
$datadir = <%= scope.lookupvar('lighttpd::params::datadir') %>
$logdir = <%= scope.lookupvar('lighttpd::params::logdir') %>
$protocol = <%= scope.lookupvar('lighttpd::params::protocol') %>
$port = <%= scope.lookupvar('lighttpd::params::port') %>

Variables used by monitor class:
$monitor_target_real = <%= scope.lookupvar('lighttpd::params::monitor_target_real') %> ( Set with $lighttpd_monitor_target )
$monitor_baseurl_real = <%= scope.lookupvar('lighttpd::params::monitor_baseurl_real') %> ( Set with $lighttpd_monitor_baseurl )
$monitor_url_pattern = <%= scope.lookupvar('lighttpd::params::monitor_url_pattern') %> ( Set with $lighttpd_monitor_url_pattern )
$monitor_port_enable = <%= scope.lookupvar('lighttpd::params::monitor_port_enable') %> ( Set with $lighttpd_monitor_port )
$monitor_url_enable = <%= scope.lookupvar('lighttpd::params::monitor_url_enable') %> ( Set with $lighttpd_monitor_url )
$monitor_process_enable = <%= scope.lookupvar('lighttpd::params::monitor_process_enable') %> ( Set with $lighttpd_monitor_process )
$monitor_plugin_enable = <%= scope.lookupvar('lighttpd::params::monitor_plugin_enable') %> ( Set with $lighttpd_monitor_plugin )

Variables used by backup class:
$backup_target_real = <%= scope.lookupvar('lighttpd::params::backup_target_real') %> ( Set with $lighttpd_backup_target )
$backup_frequency = <%= scope.lookupvar('lighttpd::params::backup_frequency') %> ( Set with $lighttpd_backup_frequency )
$backup_data_enable = <%= scope.lookupvar('lighttpd::params::backup_data_enable') %> ( Set with $lighttpd_backup_data )
$backup_log_enable = <%= scope.lookupvar('lighttpd::params::backup_log_enable') %> ( Set with $lighttpd_backup_log )

Variables used by firewall class:
$firewall_source_real = <%= scope.lookupvar('lighttpd::params::firewall_source_real') %> ( Set with $lighttpd_firewall_source )
$firewall_destination_real = <%= scope.lookupvar('lighttpd::params::firewall_destination_real') %> ( Set with $lighttpd_firewall_destination )
$firewall_enable = <%= scope.lookupvar('lighttpd::params::firewall_destination_real') %> ( Set with $lighttpd_firewall_enable )

Module base source for static files:
$general_base_source = <%= scope.lookupvar('lighttpd::params::general_base_source') %> ( Set with $base_source )
