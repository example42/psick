---
  <%= metadata.name %>::settings:
    package_name: '<%= metadata.name %>'
    service_name: '<%= metadata.name %>'
    config_file_path: '/etc/<%= metadata.name %>/<%= metadata.name %>.conf'
    pid_file_path: '/var/run/<%= metadata.name %>.pid'
    config_dir_path: '/etc/<%= metadata.name %>'
    process_name: '<%= metadata.name %>'
    process_user: '<%= metadata.name %>'
    process_group: '<%= metadata.name %>'
