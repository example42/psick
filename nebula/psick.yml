version: v0.0.1
description: Psick control-repo workflow. Testing Slack integration

parameters:
  channel:
    description: Slack channel (include preceding hashtag)
  message:
    description: Psick nebula workflow test

steps:
- name: init-workflow
  image: alpine:latest
  input:
    - echo "Initializing workflow"
- name: pdk-test
  image: puppet/pdk
  command: pdk
  args:
  - "test"
  dependsOn:
  - init-workflow
