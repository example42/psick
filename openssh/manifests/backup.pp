# Class: openssh::backup
#
# Backups openssh data directory and, optionally, logs (must be enabled)
# It's automatically included if $backup=yes
#
# Usage:
# include openssh::backup
#
class openssh::backup {

}
