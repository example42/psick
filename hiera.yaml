---
:backends:
  - yaml
:hierarchy:
  - "hostname/%{::hostname}"
  - "role/%{::role}-${::env}"
  - "role/%{::role}"
  - global

:yaml:
  :datadir: "/etc/puppetlabs/code/environments/%{environment}/hieradata/"

