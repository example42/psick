---
:backends:
  - yaml
:hierarchy:
  - "hostname/%{::hostname}"
  - "role/%{::role}-${::env}"
  - "role/%{::role}"
  - "zone/%{::zone}"
  - global

:yaml:
  :datadir: "/etc/puppetlabs/code/environments/%{environment}/hieradata"

