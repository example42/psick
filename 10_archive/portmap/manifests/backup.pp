# Class: portmap::backup
#
# Backups portmap data directory and, optionally, logs (must be enabled)
# It's automatically included if $backup=yes
#
# Usage:
# include portmap::backup
#
class portmap::backup {

}
